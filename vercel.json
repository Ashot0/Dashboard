{
	"version": 2,
	"builds": [
		{
			"src": "nuxt.config.ts",
			"use": "@nuxtjs/vercel-builder",
			"config": {
				"serverFiles": ["server-middleware/**"]
			}
		}
	],
	"routes": [
		{
			"src": "/.*",
			"dest": "/index.html"
		}
	],
	"rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
